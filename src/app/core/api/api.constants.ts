/**
 * URL base del backend NestJS.
 * En dev el backend corre en 3001 (el 3000 suele estar ocupado por otras apps).
 * Al desplegar, cambiar aquí o mover a environments con fileReplacements.
 */
export const API_URL = 'http://localhost:3001';
